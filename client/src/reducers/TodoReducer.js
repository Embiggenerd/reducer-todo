export const initialStateTodo = [{
    item: 'Learn about reducers',
    completed: false,
    id: 3892987589
}]

export const reducerTodo = (state, action) => {
    switch (action.type) {
        default:
            return state
    }
}